# Laawol 1.0.6 build 41

Released 2026-09-13.

| | |
|---|---|
| Version | 1.0.6 (build 41) |
| Commit | `f1ba86127fdf` (tag `v1.0.6+41`) |
| Tree | clean at build time |
| Flutter | 3.44.2 stable, framework `c9a6c48423` |
| Xcode | 26.6 (17F113) on macOS 26.5.1 |

Built with `--dart-define=STRIPE_PUBLISHABLE_KEY=<live pk>`, read from the
gitignored `my_flutter_app/.env.stripe.local`.

## Artifacts

| Platform | Name | SHA-256 |
|---|---|---|
| Android | `laawol-1.0.6+41-f1ba86127fdf-android.aab` | `29a36dadfe6bd8ecac25c7b245042d54f98c620b4d9a675e8ec7e2974bdeb3e3` |
| iOS | `laawol-1.0.6+41-f1ba86127fdf-ios.ipa` | `1664faaed5de4631a2cd059b38c842f2c396993584ef0f6e1be4692d2e56b505` |

Android was signed with the `upload` alias from `android/upload-keystore.jks`;
iOS with automatic signing for team GRKB7BXVZK. Play reported the bundle's
SHA-1 as `627365580390b14e9fa79778252182927ac59a83`; the App Store delivery
UUID was `960bda9f-7348-4a91-93cb-1d12e532d7af`.

## What it carries

Three pull requests since 1.0.5+40:

- **#98** brought the app level with the console on parking and the lot ledger.
  Six callables the web had been calling for fifteen pull requests were never
  called from the app: `billParkingThroughToday`,
  `recordParkingPaymentReceived`, `closeParkingStay`, `upsertLotActivityType`,
  `deleteLotActivityType`, `setExpenseProofThreshold`. It also fixed a console
  bug — a Copy button offered for a payment link the lot had already cancelled
  — which is already live on the web.
- The customer **Home** rebuilt around a "Send something home" hero.

No functions changed, so nothing was deployed to Cloud Run for this release.

## Store state

- **Play:** production, 100%, release name `41 (1.0.6)`.
- **App Store:** build 41 uploaded, version 1.0.6 submitted for review.

## Gate

`appConfig/client` after this release:

- `latestBuild` `{android: 41, ios: 40}` — iOS `latest` follows what is
  actually installable, so it moves to 41 only once Apple approves 1.0.6.
- `minSupportedBuild` `{android: 40, ios: 39}` — both point at builds that have
  been installable for a day or more. Raise them to 41 only once each store is
  actually serving 1.0.6.

## The withdraw path in `asc.py` was dead

1.0.5 (build 40) was still WAITING_FOR_REVIEW when this release went up, so
`asc.py submit` had to withdraw it first. It didn't: the withdraw still used
the `appStoreVersionSubmission` relationship, which is dead along with
`appStoreVersionSubmissions/CREATE` (retired 2026-09-10). A version submitted
through `reviewSubmissions` has no such relationship, so the GET answered with
nothing, nothing was deleted, and the version stayed locked.

The failure surfaced two calls later as a 409 on attaching the build —
*"The specified pre-release build could not be added"* — which names the build
and not the real cause.

`withdraw()` now cancels the review submission itself
(`PATCH /reviewSubmissions/{id}` with `canceled: true`) and waits: the
submission sits in CANCELING for a minute or so, and the version is not
editable until it clears. It also cancels an empty submission left behind by a
previous half-run, which would otherwise block the next one.

Note that the version keeps its identity across this: `354206d4…` was 1.0.5
carrying build 40 and is now 1.0.6 carrying build 41. Users get one version
with the newest binary, which is the intent.
