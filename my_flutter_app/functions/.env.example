# Copy to .env for local emulator/manual testing that should not charge cards.
SIMULATE_PAYMENTS=true

# Production defaults to real-payment mode when this value is absent. Only set
# SIMULATE_PAYMENTS=false explicitly in a deployment env if you want the deploy
# manifest to show that intent.
